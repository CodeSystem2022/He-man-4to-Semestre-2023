//Las anotaciones de Lombok ayudan a reducir la cantidad de código repetitivo, necesario
//para definir los métodos comunes, y las anotaciones JPA indican que esta clase puede ser mapeada
//a una tabla en una base de datos relacional.

@Entity
@Data
@NoArgsConstructor
@AllArgsConstructor
@ToString
public class Libro {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    Integer idLibro;
    String nombreLibro;
    String autor;
    Double precio;
    Integer existencias;
}
